{
  "name": "demo",
  "version": "1.0.1",
  "scripts": {
    "start": "gulp initparser && gulp && gulp vue",
    "watch": "gulp initparser && gulp && gulp vue && gulp watch",
    "build": "gulp initparser && gulp cleanall && gulp && gulp vue && gulp cleanparser"
  },
  "devDependencies": {
    "@types/node": "^11.13.10",
    "gulp": "^4.0.2",
    "gulp-clean": "^0.4.0",
    "gulp-rename": "^1.4.0",
    "gulp-replace": "^1.0.0",
    "gulp-sass": "^4.0.2",
    "gulp-typescript": "^5.0.1",
    "node-sass": "^4.12.0",
    "typescript": "^3.4.5"
  },
  "dependencies": {
    "ts-md5": "^1.2.4"
  }
}
