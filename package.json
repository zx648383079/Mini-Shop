{
  "name": "demo",
  "version": "1.0.1",
  "scripts": {
    "start": "gulp && gulp vue",
    "build": "gulp cleanall && gulp && gulp vue && gulp clean"
  },
  "devDependencies": {
    "@types/node": "^11.13.0",
    "gulp": "^4.0.0",
    "gulp-clean": "^0.4.0",
    "gulp-rename": "^1.4.0",
    "gulp-replace": "^1.0.0",
    "gulp-sass": "^4.0.2",
    "gulp-typescript": "^5.0.1",
    "typescript": "^3.4.1"
  },
  "dependencies": {
    "ts-md5": "^1.2.4"
  }
}
