{
  "name": "demo",
  "version": "1.0.1",
  "scripts": {
    "start": "gulp && gulp vue",
    "watch": "gulp && gulp vue && gulp watch",
    "build": "gulp cleanall && gulp && gulp vue"
  },
  "devDependencies": {
    "@types/node": "^12.6.2",
    "gulp": "^4.0.2",
    "gulp-clean": "^0.4.0",
    "gulp-rename": "^1.4.0",
    "gulp-replace": "^1.0.0",
    "gulp-sass": "^4.0.2",
    "gulp-typescript": "^5.0.1",
    "gulp-vue2mini": "^1.0.1",
    "node-sass": "^4.12.0",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "ts-md5": "^1.2.4"
  }
}
